<%= content_for :breadcrumbs do %>
      <ul class="breadcrumb">
        <li>Data <span class="divider">/</span></li>
        <li class="active">Employees</li>
      </ul>
<% end %>
<%= content_for :search_box do %>
      <%= form_for( @search, :url => employee_infos_path ) do |f| %>
      <%= f.select :company_id_eq, Company.all.map { |c| [c.short_name, c.id] }.insert(0, ['All Company', nil]), {}, :class => 'data-field', :onchange => 'this.form.submit()'  %>
      <%= f.select :status_eq, [['All Status', nil],['active', 1], ['leaved', 0]],{}, :class => 'data-field', :onchange => 'this.form.submit()' %>
    <div class='input-append'>
      <%= f.text_field :nickname_contains, :size => 15, :class => 'data-field' %>
      <%= search_button %>
      <%= link_to_new 'Add Employee', new_employee_info_path %>
    </div>
      <% end %>
<% end %>
<section>
<div class='data-pager'>
  <%= will_paginate(@collection, :class => 'pagination pagination-mini') %>
</div>
<table class="data-list table table-bordered table-striped" id="list_employee_infos">
  <thead>
  <tr class='table-header'>
      <th><%= t("id") %></th>
      <th><%= t("Company") %></th>
      <th><%= t("name") %></th>
      <th><%= t("roles") %></th>
      <th><%= t('telephone')%></th>
      <th><%= t('email') %></th>
      <th><%= t('status') %></th>
      <th>op</th>
  </tr>
  </thead>
  <tbody>
<% @collection.each do |e| %>
  <%= render :partial => 'row', :object => e %>
<% end %>
  </tbody>
</table>
<div class='data-pager'>
  <%= will_paginate(@collection, :class => 'pagination pagination-mini') %>
</div>

<ul class="nav nav-tabs">
  <li class="active">
    <a href="/home">Home</a>
  </li>
  <li><a href="#">...</a></li>
  <li><a href="#">...</a></li>
</ul>


