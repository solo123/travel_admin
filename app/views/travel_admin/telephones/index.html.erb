<%= content_for :breadcrumbs do %>
  Orders &gt;
  Users &gt; Emails
<% end %>
<div class="toolbar">
  <ul class="actions">
  	<li>
			<%= form_for( @search, :url => telephones_path ) do |f| %>
  		<%= f.label :tel_contains, "Tel" %> <%= f.text_field :tel_contains, :size => 15 %> &nbsp;
  		<%= button 'Search' %>
			<% end %>
  	</li>
  </ul>
  <br class="clear">
</div>

<div id="edit_div" class='dialog-normal'></div>

<div class='data-pager'>
<%= will_paginate(@collection) %>
</div>
<table class="data-list" id="list_emails">
  <thead>
  <tr>
    <th>id</th>
	<th>email</th>
	<th>data type</th>
  <th>data id</th>
	<th>created_at</th>
  <th>updated_at</th>
  <th>op</th>
  </tr>
</thead>
<% @collection.each do |d| %>
	<%= render :partial => 'row', :object => d %>
<% end %>
</table>
<%= will_paginate(@collection) %>



