var edit_div_id = "edit_payment_div";
var edit_div = '#' + edit_div_id;

if ($(edit_div).length == 0){
  $('body').append("<div id='"+ edit_div_id +"' class='modal fade' aria-hidden='true' tabindex='-1' role='dialog'></div>");
}

$(edit_div)
  .html("<%= j render(:partial => "form") %>")
  .on('shown.bs.modal', function(){
    $('[autofocus]').select(),focus();
  })
  .modal();

  //pay cash script
  $('#pay_credit_card_amount').keyup(recaculate_pay_credit_card);
  $('#pay_credit_card_amount').val($('#tb_balance_before').text());
  recaculate_pay_credit_card();

