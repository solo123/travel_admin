<%= content_for :breadcrumbs do %>
      <ul class="breadcrumb">
        <li>Accounts <span class="divider">/</span></li>
        <li class="active">Vouchers</li>
      </ul>
<% end %>
<section>
<%= will_paginate(@collection) %>
<table class="data-list table table-bordered table-striped" id="list_vouchers">
  <thead>
    <tr class='table-header'>
        <th><%= t(:id) %></th>
        <th><%= t(:amount) %></th>
        <th><%= t(:order_id) %></th>
        <th><%= t(:operator) %></th>
        <th><%= t(:expire_date) %></th>
        <th><%= t(:status) %></th>
        <th><%= t(:updated_at) %></th>
        <th>op</th>
    </tr>
  </thead>
<% @collection.each do |c| %>
  <%= render :partial => 'row', :object => c %>
<% end %>
</table>
<%= will_paginate(@collection) %>
</section>
